import { Layout } from 'antd';
const { Content } = Layout;

function Dashboard() {
    return (
        <Content
            style={{
                margin: '24px 16px',
                padding: 24,
                minHeight: 500,
            }}
        >
            Content
        </Content>
    );
}

export default Dashboard;
